# 沿海台风通项目协作指南

本文件适用于本仓库。这里是独立的台风信息网站规划项目，不是父目录中山禾餐馆网站的业务源码。

- 使用中文维护说明。区分已核验事实、拟议方案和实际验证结果。
- 当前有原生 HTML/CSS/JavaScript 演示前端与本地预览脚本，没有真实数据接口或后端业务服务。
- 页面入口为 `index.html`，样式在 `styles.css`，演示数据和交互在 `app.js`。可直接打开页面，或通过 `npm run dev` 启动本地预览；`npm run check` 检查语法，没有构建步骤。
- 本阶段按用户要求使用演示数据。全局提示、虚构名称、模拟预警及非真实地图标记必须保留；不能冒充官方发布机构。
- 实现以 `docs/项目计划书.md` 为范围基线；引入运行命令后同步更新 README。
- 官方预警必须保留发布机构、区域、发布时间、原文、状态及来源链接。
- 不从台风中心风力或距离自行生成、升级、降级、解除官方预警；地区规则不能混用。
- 模拟数据必须显著标注，不能进入实况模式或真实通知。
- 所有时间字段包含时区；区分观测时间、预报发布时间、预报有效时间和采集时间。
- 不提交密钥、个人定位、接口原始敏感响应、供应商保密材料或未经授权的气象数据。
- 重要验证应覆盖：预警状态变化、行政区域匹配、过期与缺失数据、单位和时区、移动端及弱网。
- 未执行的构建、接口联调、测试和部署不得声称已完成。
