# 沿海台风通协作指南

本文件适用于本仓库，这是独立台风网站，不是父目录餐馆项目。

- 中文维护文档，区分已实现、计划与实际验证。用户已授权真实数据接入与 GitHub Pages 发布。
- 原生 HTML/CSS/JS，Node.js 22+，无 npm 依赖；入口 index.html，交互 app.js，样式 styles.css。
- 采集脚本 scripts/fetch-data.mjs，字段验证 lib/typhoon.js，静态输出 _site/；不得直接修改输出代替源码。
- 使用 npm run check、npm test、npm run fetch:data、npm run build 验证；npm run dev 提供 HTTP 预览。
- 真实模式不能混入虚构台风或模拟预警。测试样本只能留在 tests/，不打包到站点。
- 上海路径公开接口没有地区预警数据；保持“尚未接入”，不能推算、升级、解除官方预警。
- 保留来源、时区、定位时间、采集时间，按机构区分预报。源站未单列预报发布时间，不可伪造。
- 失败保留最近成功数据并明确提示，不把网络错误当作无台风；浏览器持续检查时效。
- 不提交凭据或源站地图密钥。前端同源读取 JSON，采集由 Actions 执行，不依赖浏览器跨域请求。
- 工作流每 15 分钟计划执行，不保证准点；公开仓库 60 天无活动可能停用，README 中保留维护提示。
- 变更需检查时区、单位、缺测、跨年、异常列表、机构隔离与移动布局；不得声称未执行的检查通过。
